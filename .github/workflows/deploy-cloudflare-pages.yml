name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  build:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    env:
      SITE_URL: ${{ vars.SITE_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate SITE_URL is set
        run: |
          if [ -z "$SITE_URL" ]; then
            echo "Missing repository variable SITE_URL."
            echo "Set Settings > Secrets and variables > Actions > Variables > SITE_URL"
            exit 1
          fi

      - name: Build static site
        run: pnpm build

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    env:
      SITE_URL: ${{ vars.SITE_URL }}
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_PAGES_PROJECT: ${{ vars.CLOUDFLARE_PAGES_PROJECT }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      BUILD_OUTPUT: dist
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate SITE_URL is set
        run: |
          if [ -z "$SITE_URL" ]; then
            echo "Missing repository variable SITE_URL."
            echo "Set Settings > Secrets and variables > Actions > Variables > SITE_URL"
            exit 1
          fi

      - name: Validate CLOUDFLARE_PAGES_PROJECT is set
        run: |
          if [ -z "$CLOUDFLARE_PAGES_PROJECT" ]; then
            echo "Missing repository variable CLOUDFLARE_PAGES_PROJECT."
            echo "Set Settings > Secrets and variables > Actions > Variables > CLOUDFLARE_PAGES_PROJECT"
            exit 1
          fi

      - name: Validate CLOUDFLARE_ACCOUNT_ID is set
        run: |
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "Missing repository variable CLOUDFLARE_ACCOUNT_ID."
            echo "Set Settings > Secrets and variables > Actions > Variables > CLOUDFLARE_ACCOUNT_ID"
            exit 1
          fi

      - name: Validate CLOUDFLARE_API_TOKEN is set
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "Missing repository secret CLOUDFLARE_API_TOKEN."
            echo "Set Settings > Secrets and variables > Actions > Secrets > CLOUDFLARE_API_TOKEN"
            exit 1
          fi

      - name: Build static site
        run: pnpm build

      - name: Verify deploy artifact exists
        run: |
          if [ ! -d "$BUILD_OUTPUT" ]; then
            echo "Expected deploy directory '$BUILD_OUTPUT/' was not generated by pnpm build."
            exit 1
          fi

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ env.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          command: >-
            pages deploy ${{ env.BUILD_OUTPUT }}
            --project-name=${{ env.CLOUDFLARE_PAGES_PROJECT }}
            --branch=${{ github.ref_name }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
